---
title: Claude Code
description: Guide officiel Claude Code - Assistant IA de développement en ligne de commande
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

Claude Code est un assistant IA de développement qui vit dans votre terminal. Il vous aide à comprendre votre codebase, implémenter des fonctionnalités, débugger et automatiser vos workflows de développement.

---

## 🚀 Installation et Configuration

### Prérequis
- Node.js 18 ou plus récent
- Compte Claude.ai ou Anthropic Console

### Installation

```bash
npm install -g @anthropic-ai/claude-code
cd your-project
claude
```

### Commandes Slash Intégrées

| Commande | Description |
|----------|-------------|
| `/help` | Afficher l'aide de Claude Code |
| `/clear` | Effacer l'historique de la conversation |
| `/config` | Voir/modifier la configuration |
| `/login` | Changer de compte Anthropic |
| `/model` | Sélectionner le modèle IA |
| `/agents` | Gérer les sous-agents IA |
| `/add-dir` | Ajouter des répertoires de travail |
| `/permissions` | Mettre à jour les permissions |

---

## 🔍 Workflows Principaux

<Tabs items={['Compréhension Code', 'Développement', 'Git & Debug']}>
<Tab value="Compréhension Code">

### Analyser une Base de Code

```bash
# Comprendre la structure du projet
Explique-moi l'architecture de ce projet

# Analyser un fichier spécifique
Que fait ce composant React ?

# Tracer les dépendances
Comment cette fonction est-elle utilisée dans le projet ?

# Identifier les patterns
Quels patterns architecturaux utilise ce code ?
```

### Navigation et Recherche

```bash
# Recherche sémantique
Trouve tous les composants qui gèrent l'authentification

# Analyse de code mort
Identifie les fonctions non utilisées dans src/

# Compréhension des flows
Montre-moi le flow de données de cette feature

# Documentation manquante
Quelles fonctions nécessitent de la documentation ?
```

</Tab>
<Tab value="Développement">

### Implémentation de Features

```bash
# Nouvelle fonctionnalité
Implémente un système de notifications en temps réel

# Refactoring
Refactorise ce composant pour utiliser des hooks modernes

# Génération de tests
Crée des tests unitaires pour cette fonction

# Optimisation
Optimise ce code pour les performances
```

### Génération de Code

```bash
# Composants React
Crée un composant Modal réutilisable avec ces props

# API endpoints
Génère un endpoint REST pour gérer les utilisateurs

# Types TypeScript
Crée les types TypeScript pour cette API response

# Documentation
Génère la JSDoc pour cette classe
```

</Tab>
<Tab value="Git & Debug">

### Workflows Git

```bash
# Commits intelligents
Crée un commit avec un message descriptif pour ces changements

# Analyse de diff
Explique-moi les changements dans ce diff

# Resolution de conflits
Aide-moi à résoudre ces conflits de merge

# Pull requests
Crée une PR avec une description détaillée
```

### Débogage

```bash
# Analyse d'erreurs
Aide-moi à comprendre cette stack trace

# Debug assisté
Pourquoi cette fonction retourne undefined ?

# Tests de régression
Crée un test qui reproduit ce bug

# Monitoring
Ajoute des logs utiles à ce code
```

</Tab>
</Tabs>

---

## ⚙️ Configuration et Contexte

### Structure .claude/

Claude Code utilise un répertoire `.claude/` pour la configuration projet :

```
.claude/
├── settings.json          # Paramètres partagés (équipe)
├── settings.local.json    # Paramètres personnels
├── commands/             # Commandes slash personnalisées
│   └── [namespace]/        # Organisées par namespace
├── agents/               # Sous-agents du projet
└── CLAUDE.md            # Instructions de contexte
```

### Instructions de Contexte (CLAUDE.md)

Le fichier `CLAUDE.md` permet de donner du contexte spécifique à votre projet :

```markdown
# Instructions de Contexte du Projet

## Standards de Code
- Utiliser TypeScript strict
- Préférer les hooks aux class components
- Suivre les conventions de nommage kebab-case

## Architecture
- Respecter l'architecture clean
- Séparer la logique métier des composants UI

## Tests
- Couverture minimale de 80%
- Tests unitaires avec Jest
- Tests e2e avec Playwright
```

### Commandes Personnalisées

Créez vos propres commandes slash dans `.claude/commands/` :

```markdown
# .claude/commands/test.md

Explique comment lancer les tests de ce projet :

1. Tests unitaires : `npm run test`
2. Tests e2e : `npm run test:e2e`
3. Coverage : `npm run test:coverage`
```

---

## 📚 Sous-Agents et Spécialisation

### Gestion des Sous-Agents

Claude Code permet de créer des agents spécialisés :

```bash
# Voir les agents disponibles
/agents

# Créer un agent spécialisé
Crée un agent expert en optimisation React

# Utiliser un agent existant
@test-agent génère des tests pour ce composant
```

### Intégration MCP

Model Context Protocol pour intégrer des outils externes :

```json
// .mcp.json
{
  "servers": {
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"]
    },
    "sentry": {
      "command": "npx",
      "args": ["-y", "@sentry/mcp-server"]
    }
  }
}
```

### Bonnes Pratiques

| Technique | Description |
|-----------|-------------|
| **Contexte riche** | Donnez le contexte métier de votre demande |
| **Itération rapide** | Testez et itérez plutôt que viser la perfection |
| **Spécialisation** | Utilisez des sous-agents pour des tâches complexes |
| **Collaboration** | Traitez Claude comme un pair programming partner |

---

## 🚀 Techniques Avancées

### Automatisation

```bash
# Scripts personnalisés
/run build
/run test
/run deploy

# Tâches automatisées
configure un watcher pour les changements CSS
met en place un linter automatique

# Intégrations CI/CD
configure GitHub Actions pour ce projet
```

### Optimisation

```bash
# Analyse des performances
profile cette fonction pour les performances
optimise ce code pour la mémoire

# Bundle analysis
analyse la taille du bundle JavaScript
identifie les imports inutilisés

# Suggestions d'amélioration
suggère des améliorations pour ce code
```

### Débogage Avancé

```bash
# Debugging assisté
aide-moi à débugger cette erreur
trace cette exception jusqu'à sa source

# Logs intelligents
ajoute des logs stratégiques à ce code

# Tests de régression
crée un test qui reproduit ce bug
```

---

## 📚 Patterns et Conventions

### Bonnes Pratiques

| Technique | Description |
|-----------|-------------|
| **Prompts spécifiques** | Soyez précis dans vos demandes pour de meilleurs résultats |
| **Contexte graduel** | Commencez large puis affinez progressivement |
| **Vérification croisée** | Demandez à Claude de vérifier son propre code |
| **Documentation en continu** | Documentez au fur et à mesure |

### Workflow Recommandé

```bash
# 1. Compréhension
lis et explique la structure de ce projet

# 2. Planification  
propose un plan pour implémenter cette feature

# 3. Implémentation
implémente étape par étape selon le plan

# 4. Test et validation
teste le code et corrige les erreurs

5. **Documentation** : Mise à jour de la doc

### Tips d'Efficacité

- 📝 **Contexte CLAUDE.md** : Définissez les standards de votre projet
- 🤖 **Sous-agents** : Spécialisez pour des tâches récurrentes
- 🔄 **Itération** : Progressez par petites étapes et validez
- 👥 **Collaboration** : Utilisez Claude comme un coéquipier

---

🚀 **Claude Code vous accompagne du développement quotidien aux architectures complexes. Commencez par les workflows de base et explorez progressivement les fonctionnalités avancées.**